/* global angular */

var app = angular.module("confsearch", ["hc.marked"]);

/*

var app = angular.module("jzes", ["ngRoute"]);

app.config(["$routeProvider", function ($routeProvider) {
    "use strict";

    $routeProvider.when("/", {
        templateUrl: "views/front.html"
    }).otherwise({redirectTo: "/"});
}]);
*/